# 新版 使用须知 v6

本版不再把“自动识别滚动到底”作为唯一条件。

新的可靠流程：
1. 用户向下滚动正文
2. 正文真正最底部出现“我已阅读到这里”
3. 用户点击这个底部按钮
4. 主按钮立即变成“我已知晓并同意”
5. 用户确认后关闭使用须知

优点：
- 不依赖 scrollTop / scrollHeight
- 不依赖 IntersectionObserver 是否及时回调
- 不受 iPhone 惯性滚动、DPR、小数像素、安全区影响
- 用户能点到底部按钮，本身就证明已经到达正文底部

旧的自动到底判断仍保留为辅助功能；即使自动判断失败，也不会阻止用户完成阅读确认。

保留：
- TC2 / RSA
- 24 位设备码
- 激活码持久化
- iOS App 化防回弹
- 禁止双指缩放 v5
