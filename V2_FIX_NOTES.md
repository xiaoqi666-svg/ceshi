# v2 修复说明

本版解决两个实际 iPhone/PWA 问题：

1. “使用须知 / 更新公告”难点击
   - 整行触摸高度提升到 72~76px
   - 子元素不再抢占点击目标
   - 增加 Pointer Events 触摸兜底
   - 增加 iOS touch-action / 点击反馈

2. 设备码重启变化 / 需要重复激活
   - 设备码、随机种子、卡密使用三重存储：
     localStorage + IndexedDB + Cache Storage
   - 启动优先恢复已存在设备码，不会主动生成新码
   - 激活卡密也使用同样的恢复机制
   - 启动时请求浏览器持久化存储

3. 更新机制
   - Service Worker 缓存版本升级为 v2
   - 页面导航改为 network-first
   - GitHub 更新部署后，联网启动会优先获取新版

注意：
- 从旧版升级到本版时，如果旧版设备码此前根本没有成功写入任何持久化存储，
  第一次升级后可能需要最后重新激活一次。
- 本版稳定保存成功后，普通关闭 PWA、杀后台、重启 iPhone 均应保持同一设备码。
- 主动“清除网站数据”、删除站点数据或系统清理全部网站数据，仍可能导致授权数据丢失。
